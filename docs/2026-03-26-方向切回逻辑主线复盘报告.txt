2026-03-26 方向切回逻辑主线复盘报告

记录时间：2026-03-26 00:00:00 +08:00

一、本轮背景
在连续几轮 `post-hit guidance` 解释链迭代后，当前工程已经具备：

- rebuild debug
- profile delta
- source-shift 标签
- summary 摘要

这些工作并不是错误方向，
但继续沿这条链往下滚，已经开始出现明显的边际收益下降：

- 模型解释越来越细
- 但 belief / action likelihood / global collapse 主干没有同步增强

二、重新读取方向文档后的判断
本轮重新对照了：

- `direction/VinCi.txt`
- `direction/algorithm_direction.txt`
- `direction/algorithm_roadmap.txt`
- `direction/algorithm_remaining_optimizations.txt`

核心结论一致：

1. 当前主线应当优先补强“概率推理能力”
2. 再补强“动作似然与决策逻辑”
3. 解释层只保留最小必要，不应继续抢主线优先级

也就是说：

- 解释链先暂停
- 自动迭代从这一轮起切回逻辑主线

三、本轮确定的新迭代纪律
从这一轮开始，后续自动迭代统一按下面的纪律执行：

1. 每一轮开始前，先读取一次 `direction/VinCi.txt`
2. 再读取当前方向文档与上一轮复盘
3. 如果上一轮的“下一步建议”与主方向冲突，优先服从 `direction/`
4. 优先推进：
   - 动作证据充分利用
   - 全局公共信息坍缩
   - 固定 case / 验证体系
   - 抽黑 / 抽白接入
5. 解释层只在新逻辑变复杂到不可读时，补最小必要输出

四、接下来 10 轮的主轴
后续 10 轮不再沿 `post-hit explanation` 继续细化，
而是围绕下面几条主轴拆分工作包：

1. 行为动作似然继续增强
2. 公共信息坍缩与全局传播增强
3. 固定 case / benchmark 验证体系补强
4. 抽黑 / 抽白主流程接入

五、一句话总结
本轮的核心不是新增某个 scoring 细节，而是把自动迭代的主锚从“上一份复盘的局部后续”重新切回 `direction/VinCi.txt` 所定义的全局目标：先补强概率推理与动作似然主干，再继续决策与解释层优化。
