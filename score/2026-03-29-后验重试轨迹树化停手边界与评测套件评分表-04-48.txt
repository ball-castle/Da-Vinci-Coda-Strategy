分数：95/100
时间：2026-03-29 04:48:24 +08:00

当前问题：
1. `P(action | state)` 现在虽然已经有 `joint_action_posterior + trajectory_prior + posterior_rank_credit + posterior marginals refinement + top-gap / entropy calibration + retry_after_failure_trajectory_prior`，但本质上仍然是工程化后验近似，不是完整可学习的概率生成模型。
2. 显式 MCTS 现在已经能展开局部后续动作树，并且 continue boundary 也开始读取 `mcts depth / node count`，但仍然不是完整全局 game-state tree / 完整 MCTS。
3. `behavior benchmark` 已提升到 `average_log_margin = 0.9426689327680254`、`min_log_margin = 0.15970970043312202`，最弱 case 已明显变硬；但后验分离仍然主要来自结构化校准，不是更完整的可学习 posterior calibration。
4. synthetic self-play benchmark 现在已经不再只是 opening 视角，而且 long-horizon evaluation suite 也已经补上；但当前展示的大样本评测仍只有 `48` 局示例，不能替代更大规模、长期稳定可复跑的胜率 benchmark。
5. long-horizon evaluation suite 里 `fairness_gap = 0.015625` 已经较小，但当前示例 `seat_bias = 0.11979166666666667` 仍然不低，说明更广配置下的 player-id / opening advantage 还没有完全压平。
6. `stop / draw / risk guard` 这轮已经进一步吃到 `guard_relief_signal + search_depth_credit`，但局部 threshold 和 shaping 仍未彻底退出主决策层。
7. 项目已经非常接近收口，但离“完整生成模型 + 更完整树搜索 + 更大规模长期稳定胜率 benchmark”的最终态还差最后一段硬收尾。

git 提交信息：
- commit: ae666e7
- message: strengthen posterior retry trajectories and tree-driven evaluation suite
- time: 2026-03-29 04:48:14 +08:00
