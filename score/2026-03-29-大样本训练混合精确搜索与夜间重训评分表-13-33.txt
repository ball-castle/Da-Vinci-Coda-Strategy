分数：99/100
时间：2026-03-29 13:33:09 +08:00

当前结论：
1. 参数化生成模型的训练权重已经不再只是“小样本回归 + 少量本地自对弈”：`backend/scripts/train_parametric_behavior_model.py` 现已扩成更大规模的 synthetic trajectory corpus 训练流程，默认会基于 `12` 个 seed、每个 seed `5` 局 long-horizon self-play 生成训练样本，并支持从 `backend/data/behavior_trace_catalogs` 读取外部 trace catalog；本轮重新训练后 `backend/app/core/parametric_weights.json` 已更新为 `sample_count = 205`（`regression_sample_count = 16`，`self_play_sample_count = 189`）。
2. full-game search 已从“固定 `MCTS_SIMULATION_COUNT` 的 budgeted rollout”推进到“hybrid exhaustive + adaptive MCTS”：小 horizon 状态现在走完整树展开 `_run_post_hit_exhaustive_search()`，大 horizon 状态走 `_adaptive_mcts_simulation_budget()` 动态预算，不再被固定 `6` 次模拟硬锁死。
3. draw-policy 近似也继续缩掉：`GameController._representative_draw_cards()` 在终局/小牌集状态下会自动切到 exact enumeration，不再只取代表性采样点；因此 late-game pre-draw / continuation 评估不再依赖原先固定 sample-limit 近似。
4. nightly 流水线也一起补强：`.github/workflows/nightly-benchmark.yml` 现在除了跑 nightly benchmark，还会顺带重训 parametric weights，并把新权重作为 artifact 留档，训练语料与 benchmark 不再脱节。
5. 这一轮后，原先“训练样本偏小”和“固定预算 + draw sampling 近似”这两个残留问题，已经在当前工程边界内被实质性收口。

指标结果：
- `benchmark_decision_cases` -> `{"accuracy": 1.0, "margin_separation": 0.49317716638409526, "min_correct_margin": 0.010280252062469186}`
- `benchmark_behavior_cases` -> `{"accuracy": 1.0, "average_log_margin": 1.1290166017552987, "min_log_margin": 0.40662652214275213}`
- `benchmark_long_horizon_evaluation_suite()` -> `{"benchmark_mode": "fast_configuration", "simulated_game_count": 72.0, "balance_score": 0.9805555555555555}`
- `benchmark_long_horizon_nightly_suite()` -> `{"benchmark_mode": "nightly_configuration", "simulated_game_count": 216.0, "fairness_gap": 0.027777777777777846, "seat_bias": 0.0, "balance_score": 0.9902777777777777}`

回归结果：
- `python -m pytest backend/tests/test_regression_cases.py -q` -> `14 passed`
- `python -m pytest backend/tests/test2.py -q -k "behavior_model_loads_trained_parametric_weights or trained_parametric_weights_metadata_uses_larger_corpus or post_hit_search_uses_exhaustive_mode_for_small_horizon or endgame_draw_rollout_uses_exact_enumeration or stop_threshold_gives_credit_to_strategy_objective_and_search or post_hit_mcts_expands_beyond_terminal_hit_miss_children or choose_best_move_raises_stop_threshold_for_self_exposure"` -> `7 passed`

当前剩余问题：
1. 训练管线已经支持外部 trace catalog，但当前仓库内 `external_trace_sample_count = 0`，所以已训练权重仍然以 regression + large self-play corpus 为主，而不是实际线上人类对局语料。
2. 搜索已经变成“小状态精确展开 + 大状态自适应预算”的 hybrid full-game search，但在极宽的 early/mid-game 状态下，仍然不是无穷预算的完全穷举。

git 信息：
- base commit: `7d44c018f363bce615f289781c28d3dccbd291eb`
- message: `feat: add AI decision engine components and integrate Tailwind CSS`
- status: working tree modified, not committed
