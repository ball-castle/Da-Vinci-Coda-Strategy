分数：89/100
时间：2026-03-28 19:55:55 +08:00

当前问题：
1. `P(action | state)` 现在虽然已有 `joint_action_posterior` 和后验校准，但仍然是工程化近似，不是完整可学习的概率生成模型。
2. 显式 MCTS 已经有节点展开与回传，但当前还是浅层 `post-hit` 树，不是完整游戏状态树搜索。
3. `behavior benchmark` 已从 `0.7470` 拉回到 `0.7750`，但还没有回到更强的高 margin 区间，posterior calibration 还需要继续做。
4. synthetic self-play benchmark 已不再是假性 `draw-heavy`，但 `top1_guess_accuracy = 0.375` 仍然偏低，说明 post-draw 首猜质量还不够强。
5. 单 seed league benchmark 的 `seat_bias` 已压到 `0.078125`，但 multi-seed matrix 里仍有 `seat_bias = 0.09895833333333333`，player-id / opening advantage 还没有完全压平。
6. multi-seed matrix 里 `starting_player_win_rate = 0.5364583333333334`、`non_starting_player_win_rate = 0.4583333333333333`，先手优势仍然存在。
7. `stop / draw / risk guard` 已经更受统一目标约束，但局部 threshold 和 shaping 仍未彻底退出主决策层。
8. 项目已经进入最后收口区，但离“完整生成模型 + 更深树搜索 + 大样本长期稳定胜率评测”的最终态还差最后一段。

git 提交信息：
- commit: 43386d1
- message: 20:01 calibrate posterior, stabilize league benchmark, and unlock post-draw offense
- time: 2026-03-28 19:55:43 +08:00
