分数：93/100
时间：2026-03-29 00:48:55 +08:00

当前问题：
1. `P(action | state)` 现在虽然已经有 `joint_action_posterior + trajectory_prior + posterior_rank_credit + posterior marginals refinement`，但仍然是工程化后验近似，不是完整可学习的概率生成模型。
2. 显式 MCTS 现在已经能展开后续动作树并做回传，但仍然是局部 `post-hit` tree，不是完整全局 game-state tree / 完整 MCTS。
3. `behavior benchmark` 已提升到 `average_log_margin = 0.88020328171312`、`min_log_margin = 0.099684523007778`，但个别 case 的后验区分度仍然不够硬，posterior calibration 还可以继续做。
4. synthetic self-play benchmark 的 `top1_guess_accuracy` 已经从 `0.375` 拉到 `1.0`，但这个 benchmark 仍偏 opening / post-draw 首猜视角，当前 `recommended_draw_rate = 1.0`，不能单独代表完整长期进攻能力。
5. 标准 multi-seed matrix（`32` 局）已经把 `seat_bias` 压到 `0.015625` 且 `starting_player_win_rate = 0.5`、`non_starting_player_win_rate = 0.5`，但 weighted configuration matrix（`24` 局）仍有 `seat_bias = 0.10416666666666667`，说明更广配置下的样本规模和稳定性还不够。
6. `stop / draw / risk guard` 虽然已经进一步被统一目标和搜索信号约束，但局部 threshold 与 shaping 仍未彻底退出主决策层。
7. 项目已经非常接近收口，但离“完整生成模型 + 更完整树搜索 + 更大规模长期稳定胜率 benchmark”的最终态还差最后一段硬收尾。

git 提交信息：
- commit: 36e91ad
- message: strengthen opening posterior ranking and weight benchmark matrices
- time: 2026-03-29 00:48:31 +08:00
