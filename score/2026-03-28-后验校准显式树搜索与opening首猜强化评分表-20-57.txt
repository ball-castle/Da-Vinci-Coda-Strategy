分数：91/100
时间：2026-03-28 20:57:30 +08:00

当前问题：
1. `P(action | state)` 现在已经有 `joint_action_posterior + trajectory_prior + posterior_rank_credit`，但仍然是工程化后验近似，不是完整可学习的概率生成模型。
2. 显式 MCTS 这轮已经从终端 `hit / miss` 树推进到“后续动作树展开”，但还不是完整全局 game-state tree 或完整 MCTS。
3. `behavior benchmark` 已提升到 `average_log_margin = 0.8141`，但 `min_log_margin = 0.0858` 仍然提示个别 case 的后验区分度还不够硬，posterior calibration 还需要继续做。
4. synthetic self-play benchmark 的 `top1_guess_accuracy = 0.375` 仍然偏低，说明 post-draw 首猜质量虽然已做 opening precision 排序，但还没有真正打穿。
5. single-seed league 的 `seat_bias` 已压到 `0.015625`，multi-seed matrix 的 `seat_bias` 也压到 `0.020833333333333332`，但 `starting_player_win_rate = 0.5208333333333334`、`non_starting_player_win_rate = 0.4791666666666667` 说明先手优势仍未完全消失。
6. `stop / draw / risk guard` 这轮已经开始被 `strategy_objective_credit` 和 `search_credit` 回扣，但局部 threshold 和 shaping 仍未完全退出主决策层。
7. long-horizon benchmark 虽然已经更稳定，但当前展示量级仍只有 `48` 局 matrix，不是更大规模、多 seed、多配置、可长期复跑的系统化胜率评测矩阵。
8. 项目已经非常接近收口，但离“完整生成模型 + 更完整树搜索 + 大样本长期稳定胜率 benchmark”的最终态还差最后一段硬收尾。

git 提交信息：
- commit: e0b77af
- message: advance posterior calibration, opening precision, and deeper mcts rollout
- time: 2026-03-28 20:57:16 +08:00
