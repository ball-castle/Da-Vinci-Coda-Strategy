分数：94/100
时间：2026-03-29 03:19:16 +08:00

当前问题：
1. `P(action | state)` 现在虽然已经有 `joint_action_posterior + trajectory_prior + posterior_rank_credit + posterior marginals refinement + top-gap / entropy calibration`，但本质上仍然是工程化后验近似，不是完整可学习的概率生成模型。
2. 显式 MCTS 现在已经能展开局部后续动作树，并且 root action 也开始吃 tree score，但仍然不是完整全局 game-state tree / 完整 MCTS。
3. `behavior benchmark` 已提升到 `average_log_margin = 0.901726269184566`，但 `min_log_margin = 0.09797807022884866` 仍然说明最弱 case 的 posterior separation 还不够硬。
4. synthetic self-play benchmark 现在已经补出 `full_turn_guess_execution_rate = 1.0`、`full_turn_clean_stop_rate = 1.0`，不再只是 opening 视角；但它仍然是 synthetic benchmark，不能替代更大规模长期胜率评测。
5. stability matrix 已把多配置长期 benchmark 拉到 `56` 局，且 `seat_bias = 0.08035714285714286`、`average_starting_player_advantage = 0.026785714285714284`，比上一轮 raw configuration matrix 更稳；但这个水平距离真正“长期稳定压平先手优势”仍有差距。
6. `stop / draw / risk guard` 虽然 root 层已经更 tree 化，但 continue boundary 内部仍然残留局部 threshold 和 shaping，尚未被统一目标完全吞掉。
7. 项目已经非常接近收口，但离“完整生成模型 + 更完整树搜索 + 更大规模长期稳定胜率 benchmark”的最终态还差最后一段硬收尾。

git 提交信息：
- commit: e905b67
- message: calibrate posterior gaps and add full-turn stability benchmarks
- time: 2026-03-29 03:18:59 +08:00
