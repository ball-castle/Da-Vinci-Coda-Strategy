分数：88/100
时间：2026-03-28 19:03:20 +08:00

当前问题：
1. `P(action | state)` 现在已经有 `joint_action_posterior`，但仍然是工程化归一化后验近似，不是完整可学习的概率生成模型。
2. 显式 MCTS 已经补上节点展开与回传，但当前还是浅层 `post-hit` 树，不是完整游戏状态树或更完整 MCTS。
3. `behavior benchmark` 虽然仍是 `accuracy = 1.0`，但 `average_log_margin` 回落到 `0.7470`，说明后验层还需要继续校准。
4. synthetic self-play benchmark 依然偏 `draw-heavy`，当前 `guess_rate = 0.0`、`average_executed_steps = 0.0`，还不能单独代表完整进攻能力。
5. 单个 seed 的 league 结果仍有波动，本轮示例里 `seat_bias = 0.25`，说明只看单次 league 仍然不够稳定。
6. 虽然 multi-seed matrix 已把平均 `seat_bias` 压到 `0.0833`，但 `starting_player_win_rate = 0.5333`、`non_starting_player_win_rate = 0.4667`，先手优势还没有完全消失。
7. `stop / draw / risk guard` 已经更受统一目标约束，但局部 threshold 和 shaping 还没有彻底退出主决策层。
8. 项目已经明显进入收口区，但离“完整生成模型 + 更深树搜索 + 大样本长期稳定胜率评测”的最终态还差最后一段。

git 提交信息：
- commit: 1070158
- message: 19:03 advance joint-action posterior, explicit mcts, and matrix benchmarks
- time: 2026-03-28 19:02:52 +08:00
