from .test_fast_inference import FastInferenceEngine, FastSlot, create_mask, get_numbers_from_mask, FULL_MASK

def run_fast_inference_on_game_state(slots_data, black_pool, white_pool):
    # This acts as a bridge converting the giant objects to bitmasks and running the fast C-like engine
    pass
